# Harness Engineering 培训课程设计（8 期 × 5 分钟）

> 状态：定稿 v2.1（两轮五视角评审 + 八期逐字脚本校准后修订）。依据：`research/harness-engineering-practices.md` + 官方文档补充验证（2026-08）。逐字脚本在 `scripts/ep01–ep08`。

## 定位

- **受众**：一线工程师，每天使用 Claude Code / Cursor / Codex，已上过上下文工程课（compaction、上下文窗口、记忆管理**不重复**）。
- **不做**：harness 科普、从零写 agent loop、上下文工程复读。
- **主线**：全课程只回答三个问题，外加一个升华——
  - **别做错**（期 2-4：约束逐级变硬）
  - **算做对**（期 5-6：验证逐级拉长）
  - **别白做**（期 7：harness 自身的验证与维护）
  - **升华**（期 8：harness engineering = 组织设计）
- **每期铁律**：
  1. 一个动作打穿（30s 翻车 → 3min 演示 → 60s 原理 → 30s 判据）；无确定性机制可拍的期次改用"案例+判据"格式，不硬塞演示
  2. 每期交付一个可带走的文件/配置
  3. 结尾回答"模型再强一倍，这期内容还成立吗"（半衰期判据）
  4. 结尾 20 秒 Cursor/Codex 对应位置卡片（但明说这是 Claude Code 主修课）
  5. 作业带外部判据（"跑这个脚本，过了才算完成"），不靠自我报告验收
- **脚本写法纪律**（第三期校准确立，八期已统一执行）：
  1. 口播只承载因果链；协议细节（exit code 语义、字段名、配置语法、出处、数据清单）一律放字幕卡让观众自读
  2. 高潮/反转前 3-10 秒预埋心智模型（范本："但注意，这时候我们只锁了前门"）
  3. 一个约 25 秒的段落只讲一个新概念

## 事实纪律（脚本必须遵守）

- METR 数据讲"任务 horizon **翻倍时间**"（不是"半衰期"），带 50% vs 80% 可靠性双数字 + "任务集偏干净可自动评分"限定（可放字幕卡，但口播必须有指向）。
- "模型吸收不了验证"的论证走**激励独立性**（自己判自己必然放水，是结构问题不是能力问题），不说"模型做不到"——Opus 4.6 起自我验证在增强。
- 模型的定性表述用"它是优化器，**优化的是你给的信号而不是你的意图**"，不用"自利的优化器"这类不可证伪的拟人化断言。
- "84% 弹窗减少"署名 Anthropic 官方博客自报，度量的是弹窗次数不是安全事件。
- "可靠性是复合概率"标注为直觉模型（假设独立步，实际有自我纠错，没这么惨也没这么好）。
- OpenAI "~100 行 AGENTS.md"必须和背后的 docs/ 体系一起讲（用于第二期"写什么还剩"微段）。
- **CLAUDE.md 效能证据存疑**（arXiv 2602.11988：context files 总体不提升成功率、推理成本 +20%）——本课教机制，不为"多写规则"背书，此事实必须在第二期如实呈现。
- `/goal` 已一手验证：Claude Code v2.1.139+（docs.anthropic.com/en/docs/claude-code/goal），本质是 **session 级 prompt-based Stop hook 的官方快捷方式**（hooks 参考文档原话）；Codex Goals 见 developers.openai.com cookbook（2026-05）。`/loop` 为 Claude Code 内置 skill（时间驱动）。
- 不使用无出处的精确数字（如"5% 概率无视"）——改为"概率不为零"。
- "bash 绕过 hook"表述为"社区已报告的绕过模式"，不做确定性断言。
- 第八期的三层对抗视角标注为**本课提出的分析视角**（组织理论与古德哈特定律的应用），不伪装成有现成文献背书的定论；变异测试工具本身成熟（mutmut/Stryker），但其在 agent 场景的应用需在脚本前实测验证。

## 制作纪律

- **实录只拍确定性机制**（hook 拦截、权限拦截、Stop hook 验收、变异测试——100% 可复现，一条过）。
- **prompt 驱动型机制豁免条款**：`/goal` 这类 prompt-based 机制，**配置面实录、判定行为走策展 transcript**（它的判定是模型评估，不属确定性机制）。拍摄第五期（shell 型 Stop hook，确定性）与第六期（prompt 型，概率性）时不得混用标准。
- **概率性失败用二手材料或策展 transcript**（标注"选自 N 次运行中的典型样本"），不赌现场复现；每个翻车镜头必须有 fallback B 方案。
- **攻击画面零实拍**（注入 payload 只讲架构不演示攻击）。
- `.env`、`rm -rf` 等镜头不出现在录屏中；翻车叙事改为"它试图 X，被拦下了"。
- 查表内容（权限语法、deny/ask/allow 决策表、PostToolUse 配置细节）配 cheatsheet 图文卡，不占视频时长。
- terminal 录屏注意手机端可读性；等待过程允许"已剪辑"诚实字幕。

## 配套资产

- **模板仓库**（全系列收口）：CLAUDE.md 骨架 + 3 个 hook 示例 + settings.json + promptfoo 冒烟配置（**第二期起随仓库下发**，让每期改动从一开始就可度量）+ 期 8 的 subagent 组织示例。每期视频给对应目录链接。
- 每期结尾一个 ≤10 分钟、在观众自己 repo 可执行、带外部判据的作业。

---

## 第一期：立论——为什么模型越强，越要谈 Harness

**回答**：值得做。

- **开场 hook（视频内 40s，完整实录放视频简介链接）**：agent 报告"全部测试通过"，实际测试根本没跑。一个范式讲透，不搞五范式编目。**B 方案**：现场录不到就换公开事故二手材料（GitHub issue/社区截图），脚本两种版本都备。
- **两层归因**（诚实版，不撒谎说"全是结构性"）：
  - 能力层的概率性失效（谎报、约束漂移、长程衰减）——**会随模型代际缓解**
  - 激励与信任层的错配（优化你给的信号而非你的意图；自我验收；越权）——**永远在**，因为信任是关系不是能力
- **核心论证**：模型变强改变的是能力，改变不了它优化的是信号；官方一边模型变强一边收紧权限（v2.1.183 硬阻断）就是证据。
- **给出全系列唯一理论工具**：半衰期判据——"这个实践补偿的缺口，模型下次升级后还在吗？"
- **交付物**：判断框架 + 系列地图（别做错 / 算做对 / 别白做 / 组织设计）。

## 第二期：约束一级——CLAUDE.md 管"在不在场"

**回答**：别做错 · 第一级。

- **两个机制属性**（主线）：
  1. **加载机制**：用户级/项目级/目录级分层、`.claude/rules/` 的 paths 作用域、import 语法——规则写得再好，作用域配错等于没写
  2. **强制力**：建议性、概率服从——概率不为零的失效你承受得起吗
- **"写什么还剩"微段（90s，归属本课，不踢给上下文工程课）**：实证开路（arXiv 2602.11988 + 官方 Exclude 清单）→ 幸存三类（坑 / 理由 / 非默认约定）→ `/doctor` 现场修剪一份 300 行 CLAUDE.md（确定性 demo，一条过）。删减判据一句话："删掉这条它会犯错吗？"
- **动作**：把一条真实规则（"别改 `generated/` 目录"）配到正确作用域。
- **失效现场**：作用域配对了，它依然可能无视——后果承受不起 → 必须升级。
- **判据**：加载机制是产品设计，会变；"约束需要分层级生效"不会变。
- **交付物**：分层的 rules 目录结构 + 模板仓库（含 promptfoo 冒烟配置，本期下发）。
- **作业注**：与第一期作业形成基线对比（第一期扫会话记录建基线，本期配完规则后重扫对比）。

## 第三期：约束二级——Hooks 管"拦不拦得住"

**回答**：别做错 · 第二级（全系列实操旗舰期）。

- 接第二期同一条规则，当场升级为 PreToolUse hook，确定性拦截。
- **视频只打穿一件事**：PreToolUse 保护 `generated/`——含高潮一幕：**演示绕过（用户显式下令"用 sed 改"，绕过行为 100% 可复现、一条过）**，matcher 必须覆盖命令模式。这是博弈设计，不是配置清单。（模型自发绕过的版本只作策展 transcript 备用。）
- **拦截信息写法**：说明为什么 + 正确替代动作——塞回的信息是写给模型看的 prompt，不是给人看的报错。
- **PostToolUse 反馈型**（lint `--fix`、输出截断）与**验收型**（Stop）：视频一句带过，配置成品放模板仓库/cheatsheet；Stop hook 留给第五期展开。
- **判据**：hook 编码"模型做不到 X"的假设，模型升级后要审计；但"概率服从 vs 确定执行"的差值永远存在。
- **交付物**：settings.json 里的 PreToolUse 保护 hook + verify 判据脚本（PostToolUse 示例在仓库）。

## 第四期：约束三级——权限与沙箱管"谁兜底"

**回答**：别做错 · 第三级。

- **动机前置（lethal trifecta，60s，零攻击实拍）**：私有数据 + 不可信输入 + 外发通道三者齐备 = 拓扑性风险，模型再强也解决不了——这是"为什么必须焊死环境"的存在理由，先讲。
- **弹窗悖论**：审批疲劳 → 无脑 allow → YOLO 模式。消耗人注意力的安全机制会自我瓦解。
- **核心翻转**：信任决策从运行时（问人）挪到设计时（焊死环境）。沙箱双隔离后弹窗减 84%（官方自报），剩下的弹窗才有人认真看。
- **双隔离缺一不可**：只隔文件 → 数据从网络走；只隔网络 → 读 `~/.ssh`。安全边界画在**损害路径**上，由最坏情况倒推。
- **deny / ask / allow 决策框架**：主体移 cheatsheet 图文卡，视频只留一句话口诀（"deny 一次都不能发生，allow 沙箱能兜底，ask 只留人看得懂的"）。
- **反直觉点**：模型越强，官方越收紧——能力涨均值，安全看尾部。（第一期已用 v2.1.183，本期增量证据用 v2.1.215/218 收回自主触发权。）
- **判据**：信任边界不可吸收，本期内容只会升值。
- **交付物**：项目级 settings.json + 沙箱配置（决策表走 cheatsheet）。

## 第五期：验证一级——一次任务怎么算"完成"

**回答**：算做对 · 单次。

- 回扣第一期的谎报完成：解法不是"更强调要跑测试"（概率域），而是**"完成"的操作性定义权收归 harness**（确定域）。
- **Stop hook 验收**（确定性 demo）：拦截退出、跑验证、失败塞回。三个设计决策：拦什么条件（只在失败时拦）、跑什么检查（40 分钟全量不能进热路径，选快代理信号）、兜底（最大轮次防死循环）。
- **博弈闭环**：它会删测试让验证通过 → 测试文件保护（复用第三期 PreToolUse 机制，一句话换个路径）——验证信号必须独立于实现。
- **verifier 分离**：生成和评估同分布，自己判自己必然放水（激励问题，非能力问题）。
- **Playwright MCP（30s 成品对比截图，不拍过程）**：反馈信号保真度升级——从"代码看着对"到"渲染结果对"。
- **判据**：ground truth 在外部世界，本期永不过期；具体信号形式会变。
- **交付物**：Stop hook 验收配置。

## 第六期：验证二级——自主循环的"完成判据"

**回答**：算做对 · 长期。长任务工程 = 把一个大"完成"切成一百个小"完成"。

- **开场考古（≤20s，准确表述）**：外部循环的形态迭代——手搓 bash while（Ralph）→ 官方插件（ralph-wiggum）→ 内置命令（`/goal`）。**技巧在迭代，模式在沉淀**。（注意：不说"Ralph 之死"——Huntley 本人认为该模式是 generic 的，官方也仍在维护插件。）
- **机制揭秘（30s）**：`/goal` 不是黑魔法——官方文档原话，它是"session 级 prompt-based Stop hook 的快捷方式"。**就是你第五期手写的东西的官方封装**。Codex Goals 同样把完成条件暴露为配置面（what should be true + how success should be checked）。
- **正菜：完成判据（completion criterion）怎么写**：
  - 写太松 → 第一轮就宣布达成（第一期"谎报完成"换皮重现）
  - 写太死 → 永远停不下来，烧穿预算
  - 可验证的判据 = 外部信号（测试通过、build 成功、文件存在）；不可验证的判据 = 新的谎报入口
- **失效模式（两个）**：agent 博弈判据（Goodhart 首次登场，为期 8 埋引线；goal drift 一句并入）、**无人值守质量回退**（Ronacher 的一手警告——上不上循环结构的关键输入）。
- **选择判据**：`/goal`（目标驱动）vs `/loop`（时间驱动，轮询类）vs 手动交接三件套（horizon 之外的超长任务——**一句话轻量提及**，全系列不展开，详见调研报告 §4.1 与模板仓库）。
- **horizon 判断**：METR 任务 horizon 翻倍时间（带双数字和限定）——先判断任务在不在模型 horizon 内，再决定上不上循环结构。
- **判据**：`/goal` 的形态会继续变（它自己就是 Ralph 的转世），但"自主循环的可靠性 = 完成判据的可验证性"不会变。
- **交付物**："可验证完成判据"写作清单 + `/goal` 好判据 vs 坏判据对照示例。

## 第七期：元层——Harness 本身的验证与维护

**回答**：别白做。

- **evals**：改完 harness 怎么知道变好了？promptfoo 实跑（$0.05 案例；冒烟配置第二期已随仓库下发，本期扩容）；capability evals（爬坡）vs regression evals（防倒退）；换模型时 days vs weeks 的差距。
- **呼应每期作业**：期 2-6 的改动本期统一度量——观众此刻手里已有六份配置和六份作业结果，evals 是给它们补的体检。
- **harness as code**：`.claude/` 入库，团队共享、版本演进（Thoughtworks Adopt 级背书——放字幕卡）。
- **Build to Delete**：每个 hook/规则编码了一个会过期的假设——定期审计：模型升级后，这个约束防的事模型还会干吗？过期的约束不是留着无害，是纯成本（稀释 + 拖慢热路径）。
- **判据**：evals 是唯一半衰期无限的能力——模型越强，越需要知道旧假设死没死。
- **交付物**：promptfoo 配置 + 一份"harness 审计清单"。

## 第八期：升华——把 Harness 当组织来设计

**回答**：这门学科是什么。（本期的三层对抗视角为本课提出的分析框架，脚本需如实标注。）

- **核心翻转**：人类组织设计就是面向"智能但不可靠个体"的 harness engineering，迭代了几百年。职责分离、最小授权、轮班交接、独立审计、成文制度——前七期每个机制都有一个活了上百年的名字（对照清单放画面连线图，观众自读）。
- **三层对抗（路标先行：一共三层，只讲第二层）**：
  1. 开发 vs 测试（=第五期 verifier 分离的组织学表达，明说"讲过，不重复"）
  2. 审计 vs 第一层的合谋——**本期唯一正菜：变异测试**（往代码里植入 bug，看测试套件抓不抓得到）——"测试测试的人"
  3. 局部 vs 全局：每个 subagent 的 prompt 是它的 KPI；局部最优 = 全局死亡——harness 最深的一层是激励设计，与 alignment 的 scalable oversight 同构
- **Demo（守制作纪律）**：v3 变异测试**实拍**（工具行为，确定性，一条过）；v1/v2 用策展 transcript，明示"选自 N 次运行的典型样本"，v1 可直接回扣第一期素材（形态需与第一期最终版本对齐）。
- **动作**：subagent 双岗位结构（implementer + 盲测 reviewer）——仓库成品 + 30 秒导览，不当场教学。诚实限定：盲测的"盲"是概率机制（subagent 无路径级 Read 权限），所以变异测试这道确定性审计才是兜底。
- **结尾三个开放问题（不给答案）**：
  1. 对抗层数有没有尽头？——谁来审计审计者；人类组织的诚实回答是"只有你愿付多少对抗税"
  2. 全局目标能不能被编码？——你的公司和 alignment 研究共同未解
  3. 模型变强后对抗结构还需要吗？——区分两种失效：**能力型失效**（谎报、跑偏）会缓解；**激励型失效**（优化信号而非意图的博弈）随能力升值。但对抗层的具体形态同样会过期——这是本课的边界条件，不是永恒定律
- **交付物**：subagent 组织示例（仓库成品）+ 模板仓库完整收口。

---

## 期数结构速查

| 期 | 问题域 | 标题 | 交付物 |
|---|---|---|---|
| 1 | 值得做 | 为什么模型越强，越要谈 Harness | 半衰期判断框架 |
| 2 | 别做错·一级 | CLAUDE.md 管"在不在场" | 分层 rules 结构 + 模板仓库 |
| 3 | 别做错·二级 | Hooks 管"拦不拦得住" | PreToolUse 保护 hook |
| 4 | 别做错·三级 | 权限与沙箱管"谁兜底" | settings.json + 沙箱配置 |
| 5 | 算做对·单次 | "完成"的定义权 | Stop hook 验收配置 |
| 6 | 算做对·长期 | 自主循环的"完成判据" | 判据写作清单 + `/goal` 示例 |
| 7 | 别白做 | Evals 与 Build to Delete | promptfoo 配置 + 审计清单 |
| 8 | 升华 | 把 Harness 当组织来设计 | subagent 组织示例 |

## 开拍前 checklist

- [x] 第三期 sed 绕过镜头：2026-08-06 实拍验证通过（`spike/ep03-demo` + captures）。要点：温和指令会被拒（模型引用规则 preemptively 拒绝），必须用脚本校准后的强硬指令版；v1 绕过、v2 拦截、Edit/cp 拦截+改道三拍均录到 transcript。脚本 Step 2/3 已按实测修订
- [x] 第八期变异测试：已实测（`spike/NOTES.md`，2026-08-06）。结论：mutmut/PIT 工具链确定性成立、可一条过；但**agent 生成套件对"小函数+规约明确"已压不出逻辑洞**（3 次独立运行全部杀死逻辑变异），演示 A 的"全绿但有洞"必须走策展 transcript 或换更复杂任务；手写弱套件的洞 100% 可检出（兜底素材就绪）；存活变种稳定含报错文案类，是现成的"分诊"教学素材。追加（2026-08-07）：盲测双岗位两轮采样（阶梯电价/图书馆逾期费）同样无逻辑洞，v2 采集策略已回填第八期脚本
- [x] 第一期翻车实录：2026-08-06 四轮采样 4/4 全诚实（含误导前提、长任务变体）——这代模型简单任务压不出谎报。脚本已改：二手素材升级为首选，实拍需更高任务复杂度 + N≥10；v1 素材形态与第八期回扣对齐待二手素材选定后核对
- [x] `/goal` 语法已复核（2026-08-06，官方 goal 文档）："session 级 prompt-based Stop hook 的快捷方式"原话一致；`/loop` 时间驱动经官方对比表确认。增量事实（评估器默认 Haiku、只读 transcript 不独立跑命令、判据上限 4000 字符、支持 `claude -p`）已回填第六期脚本。追加（2026-08-07）：松/死判据双样本已实跑取材（镀金蔓延 25 轮 $0.41 / 7 分钟 27 轮强杀），口播虚构数字已按实测改写。产品快速迭代期，开拍当月再复核一次
- [x] 沙箱 settings.json 的 `sandbox` 字段 schema 已对照官方 sandboxing 文档复核（2026-08-06）：修正 `network.allow` → `network.allowedDomains`；确认 `sandbox.filesystem.*`、`sandbox.credentials`（v2.1.187+，~/.ssh 默认可读需显式 deny）、Linux 依赖 bubblewrap+socat。开拍当月再快速复核一次
- [x] ~~`/doctor` 修剪的交互流程实测~~ 已取消（2026-08-07 决议）：①该段教学核心是删减判据，不依赖 /doctor，脚本已有等价 B 方案（按官方 Exclude 清单手工修剪，同样确定性）；②/doctor 的修剪提议是模型生成内容，本来就只有 UI 流程是确定性的；③版本门槛特性（v2.1.206 起，本机 2.1.170 没有）且快速迭代，提前实测到开拍也过期——开拍时确认版本 ≥2.1.206 就用 /doctor，否则直接走 B 方案，无需提前实测
- [x] Stop hook 的 stdin 字段已复核（2026-08-06，官方 hooks 参考文档）：原生 `stop_hook_active` 存在，但语义是"只拦一轮"，与第五期"最多拦 3 轮"的设计不同——保留自实现计数器（已补"通过即清零"修复 + 拦截审计日志；harness 有连续拦截上限兜底）。第五期拦截实录已拍（106 秒完整闭环）。开拍当月仍需快速复核字段未改名
- [x] promptfoo 口径已对齐并全量实跑（2026-08-07）：冒烟版与扩容版一致；`fixtures/demo-repo` 夹具补齐（baseline 快照 + extension 逐用例还原 + 哈希判据）；断言全部确定性化（javascript metadata / python 重跑测试），9 条踩坑记录见 `spike/captures/README.md`
