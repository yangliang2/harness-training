# 反范式课程 · 候选清单 v2（按新标尺重写）

> 状态：候选池 v2（2026-08-19 两轮校准后重写）。定位：与主课完全独立；8 集 × ≤5 分钟；一集一个反范式。
> 标尺（用户定）：①颗粒度=能点名的具体物件/行为 ②有自查（观众 5 分钟查自己项目）③有整改（当天做完、改完可验证）④代价可量化 ⑤不教蠢错、不讲常识 ⑥独立、耐升级。
> 状态图例：💬 讨论中 / ✅ 纳入 / ❌ 出局

## 候选池

### ✅ 1. 配置只加不减（环境体检集）【已纳入，方案 B 合并版】
反范式 = MCP / skill / plugin / CLAUDE.md 只进不出，常驻即税。装的时候都觉得有用，从没删过。
- 核心工具：`/doctor`（官方命令参考已核实，2026-08-19）——一次体检列出"闲置 vs 上下文成本"（MCP/skill/plugin）+ CLAUDE.md 修剪去重；先出报告、改前逐项确认。CLAUDE.md 修剪需 v2.1.206+（本机 2.1.226 满足）。
- 背景事实：MCP Tool Search（工具描述超 10% 上下文时按需加载）存在但自动触发有失灵报告（issue #47645 / #18298），不能当兜底——口播一句带过，数字以开拍实测为准。
- 自查：跑 `/doctor` + `/context` 看占用。
- 整改判据：上周用过几次？零次就删；CLAUDE.md 口诀"它自己能从代码推出来的，不用你写"。
- 交付物：环境体检清单。
- 待实测（开拍前）：①装 N 个 server 的 `/context` 实测占用；②Tool Search 本机是否触发；③`/doctor` 体检全流程实录（其修剪建议是模型生成内容，拍摄按"配置面实录、建议走策展"处理）。

### ❌ 2. 规则臃肿症 → 已并入 #1（方案 B 决议，2026-08-19）
CLAUDE.md 臃肿作为"只进不出"的一个类目在 #1 内覆盖，删减判据以口诀形式保留。

### ✅ 3'. 工具输出无节制【已纳入，2026-08-19 重塑后纳入】
原版"全量倾倒"（人贴超长文本）被否：稻草人场景、对比实验不诚实。重塑为工具侧：给 agent 配的工具/脚本输出从不节制——`make test` 全量 4000 行、自建 MCP 一次返回整表 JSON、hook 塞回完整 lint 报告。人一条没贴，上下文照样被吃掉大半。
- 定位金句：工具返回又臭又长 = 你的环境设计差，不是模型的病。
- 自查：`/context` 看 tool output 占比；翻会话找单次最长工具返回。
- 整改（本集干货重心，用户亲定）：①**PostToolUse hook 预处理工具返回**（截断/过滤后再进上下文）②验证命令加安静模式（默认只输出失败项）③自建 MCP 工具强制 limit + 字段裁剪。判据：任何工具的默认返回，一屏装得下。
- 翻车 demo（待实测）：同一诊断任务对照——verbose 全量输出组后半程忘约束 vs 安静模式组；`/context` 占用对比。
- 交付物：工具输出体检清单（对着 Makefile / MCP server / hook 逐项查）。

### ✅ 4''. 长会话当仓库 —— 状态从不落盘【已纳入，2026-08-19 定稿】
反模式 = 长会话：决策、进度、约定全存在对话历史里，从不落盘，靠 /compact 无限续命。
- 反面素材：OpenClaw 失忆案——整个架构为解决失忆而生（memory flush + MEMORY.md 免疫压缩），社区照样"失忆"成固定话题；根因：flush 是模型自己决定写什么，概率机制。结论：**机制兜底是概率的，亲手落盘是确定的**。（OpenClaw 只作反面素材，不展开其产品用法。）
- 两笔账：①compact 是有损压缩，开头约束会被压没；②每轮成本随上下文线性涨，续杯越续越贵。
- 可执行整改（实战版，2026-08-19 用户校准后定稿）：①**装仪表**：`/statusline` 加上下文占用百分比进度条（官方原生支持，2025-12 起，零安装；第三方 ccusage/Claude HUD 可选但不推）；②**定阈值**：60% 开始准备 HANDOFF.md、70% 必须开新会话——compact 是事故不是信号；③**交接落盘**：HANDOFF.md 四格模板，agent 起草、人过目；④**兜底**：PreCompact hook 压缩前自动触发写盘。流程一句话：看着表开车，到线就换班，撞墙有安全气囊。
- 验收（外部判据）：compact 前后让 agent 复述约束对照 + 新会话读 HANDOFF.md 能否零提问开工。
- 交付物：HANDOFF.md 模板 + PreCompact hook 配置。
- 待实测：compact 丢约束走"压缩摘要前后对照"（确定性版本）。

### ❌ 5. 全 allow / YOLO 常驻【出局，2026-08-19 用户否决：选题不好】
疑似原因：与主课第四期（权限/沙箱）同源，偏安全配置而非使用习惯。#14 插件盲装同属安全配置向，待用户一并确认。

### ❌ 6. 裸改不存档【出局，2026-08-19 用户否决】
连同合并提议（与 #12 合并）一并否决。校准出的隐藏标准：**反范式必须是"用了 AI 才有的病"，或至少在 AI 场景下被急剧放大**——commit 纪律是通用工程素养，不合格。

### ✅ 19. 逆分布造轮子【已纳入（用户确认 2026-08-19），最后一集】
反模式 = 自研框架/非标 MCP/冷门链路，逼 agent 在训练分布外裸泳：模型对主流栈是闭卷考试，对你的自研栈是当场现学。
- 反面出局记录：A"无脑用最强模型"（过度思考贵且差）痛点真实但**拍不出对照**，否决。
- demo：`gh run view`（肌肉记忆一次到位）vs 自研 ci-lookup MCP（编错参数/记错工具名/重试三轮）——成功率走 N≥10 采样，如实标注分布。
- 整改（三档模型税，税从低到高）：①直接用主流工具（gh/git/npm/docker 级，税为零）②**兼容外壳**——内部工具实现主流同款接口（先例：GitLab 的 glab 照 gh 做；边角差异写进 --help，模型自己会读）③完全自研接口：税最高，必须配文档对冲。
- 选型判据一句话：让它裸写一段调用——一次写对零税，查文档能写对低税，查完还编错高税。
- 诚实边界：不是禁止自研，是自研计模型税。
- 交付物：模型税选型判据卡。

### 💬 7. 人肉传话筒【未入选，存档】
收窄版（给它够不到的信息源接感官）未通过最终评审；与全集其余集重叠风险未消。

### ❌ 12. 一把梭 / ❌ 11. 裸奔【未入选，存档】

## 已出局（按新标尺淘汰，存档备查）

### ❌ 8. plan 盲批 / 散文当 spec【整集出局，2026-08-19 用户否决：痛点不成立】
历经六轮重构（说教检查→限长+证据→grill 攻击清单→验收工件→拆小→结构化工件），最终反模式锚定为"散文当 spec"后被否决：重型任务有 SDD 框架（spec-kit/Kiro/OpenSpec）焊死结构，小任务不需要 spec，剩下的中间档痛点太弱，撑不起一集。调研与 spike 资产保留（`research/plan-review-practices.md`、`research/spec-visual-review-evidence.md`、`spike/anti-plan-diagram/`），"列未决问题"等招式可作其他集的配料。
**累积校准标准**：④反模式必须有真实痛感的人群基数——被成熟工具链覆盖的领域不算。

### ✅ 9/10. 空头支票 —— 无脑信任模型输出【已纳入（用户逐字确认 2026-08-19），吞并 #9/#10】
反模式 = 把"说"当"做"：汇报/声明/承诺都是描述世界的生成物，不伴随工件变化一律按"没发生"处理。
- 三种形态：A 顺手改动（汇报少报改动）/ B 状态声明走私（"已确认"无原话，本课程开发中真实发生两次）/ C 承诺性空话（"我会改正"零落盘，用户高频痛点）。
- 关键教学点：可见性不对称——"多出来"的异常面板可见，"没发生"的异常只能在发生时刻登记/拦截。C 类无事后解，只有登记制 + 无效化（"没落账的承诺按不存在处理"）。
- 整改架构：①`/diff` + 三个高危时刻（宣布完成/话题切换/状态汇总）主管 A/B——原生命令零配置；②correction-guard.py（C 类必需）：Stop hook 检测**用户纠正口头禅**（模型无关），纠正后无落盘且无一次性声明则 block，已脚本级实测 6/6（`spike/anti-status-smuggle/`）；③verify-confirm.py（可选硬化）：PreToolUse 拦截无用户原话的 ✅ 写入，实测 4/4。
- 接线：注册 settings.json Stop 事件；`stop_hook_active` 只拦一轮，复用主课 ep05 自实现计数器模式。
- 待实测（开拍前）：真实会话端到端 N 次（transcript 结构/拦截上限/误报率）。
- 诚实边界：规则对模型是概率的（本课程开发中实测：刚写的规则两轮内被违反）；C 类无银弹，交付的是"让它伤不到你"。
- 交付物：双 hook + settings.json 片段 + 三扳机卡。CLAUDE.md 不是万能的——机制层兜底。

### ❌ 10. 每次从零重讲【已并入 #9/10 空头支票，2026-08-19】
纠正不落盘 → 下周重犯，是"口头改正不落盘"的下游症状。

### ❌ 13. 并行空转【出局，2026-08-19 用户否决：没有好解法】
review 带宽问题无硬解法。成本维度由 #18 继承（多会话切换 = 缓存全灭）。

### 💬 14. 插件盲装
插件市场/marketplace 一键装包，里面塞了什么 skill、什么 hook 从没看过。
自查：列出已装插件 + 逐个看包含文件。整改：只装能逐行读完的包；第三方 hook 当代码审。

### ✅ 15. 文档坟场（原"过度喂料"重定义）【已纳入，2026-08-19 定稿】
反模式 = agent 可读的 repo 语料从不打扫：历年故障复盘、过时调研、wiki 导出堆在 docs/，agent 探索时自行撞见。同源两害：①稀释（关键文档被尸体埋掉）②喂毒（过时结论被当现状——它分不清现状和考古现场）。
- **翻车 demo（确定性）**：布景 repo 埋过时调研（结论与现实相反），任务"给缓存加过期策略"→ 按旧结论出牌，一条过。
- **自查**：用 agent 视角 grep 三个核心关键词看撞出几份尸体；或直接问"你这个结论从哪读到的"。
- **整改**：治 = doc-audit skill（交付物，逐份验证"文档引用的接口/文件在代码里还存在吗"，产出带证据的分诊表，人只批量批表）；防 = CLAUDE.md 两行（"文档与代码冲突以代码为准" + "archive/ 是考古区别读"）。
- **诚实声明（用户亲定，必须讲）**：①此方法只能改善不能保证——"引用的代码还在但结论已过时"的文档会漏网；②**立场是劝退这条路**：预防为主，复盘/历史文档别堆进 repo 正文，audit 是给已堆的人擦屁股，不是许可证。
- 与 #1 边界：#1 管常驻配置（每会话必加载），这集管可检索语料（探索时撞见）。
- 只讲"塞太多"，#11 裸奔不并入（用户决议）。

### ❌ 16. 失败就"再试一次"【不进池，2026-08-19 用户否决】

### ✅ 17. 一个会话炒三盘菜【已纳入（用户确认 2026-08-19，四出口版）】
反模式 = 一个会话混多个任务：支线话题全部进上下文——①正线被稀释（典型上下文污染）②最终 diff 混入不相关改动，review 未必能发现。
- 翻车 demo（确定性）：主线中途插入"顺便改下 README 错别字"→ 最终 diff 躺着 README，合入即夹带；/btw 组 diff 干净。
- 整改（判据一句话）：和当前任务无关的话，不进这个会话。**四个出口全原生命令**：问→`/btw`（不进上下文）；做且要当前上下文→`/fork`（复制会话后台跑，零污染，v2.1.221+ 自动建议 worktree）；做且要结果回报→`/subtask`（结果回当前会话，代价是带回少量上下文）；做且无关→新会话。
- 诚实边界：/btw 只救"问"不救"做"；/fork、/subtask 为 v2.1.212+ 新命令，开拍前复核行为与版本门槛。
- 交付物：四出口判据卡。
- 与 #4'' 边界：#4'' 管"状态不落盘"，这集管"任务混营"；#4'' 教到线换班，这集教支线分流。

### ✅ 18. 僵尸会话复活（原"挂会话丢缓存"精化）【已纳入，2026-08-19】
反模式 = 任务死了会话还挂着，回来随手一问，为 10 万 token 的僵尸上下文付全价。挂本身无罪，复活僵尸才是病。
- 关键澄清（用户校准）：新会话同样无缓存优惠，省钱来源是**新会话小**（只读 HANDOFF 一页纸）不是缓存；判据 ="这个会话的上下文还值一次全价吗"。
- 时间轴决策卡：<5min 接着干 / 5–50min `/loop 4m ok` 保活（官方内置，无需第三方 keepalive）/ >50min HANDOFF 收工。keepalive 盈亏平衡 ≈50 分钟（写 1.25x、读 0.1x，每小时保温 = 1.5 次全价 > 复活 1.25 次）。
- 数字卡（已核实官方定价页 2026-08）：Claude Fable 5 缓存价差 10x（复活 10 万 token ≈ $1.00）；Kimi K3 10x（$0.30，缓存自动）；GPT-5/Gemini ~10x；DeepSeek V4 全系 **30x+（叠峰谷 60x）**——官方定价页实锤，用户印象纠正了二手综述的 10x 误记。点睛：价差是厂商在惩罚僵尸复活。
- 诚实边界：订阅用户体现为额度非账单；这集价值随模型档位伸缩（DeepSeek 绝对值无感，但 30x 激励结构最强）；开拍前复核定价页。
- 待实测：`/loop` 保活后 `cache_read` 命中验证（不通过则删中间档，留两档）。
- 与 #4'' 分工：#4'' 讲"怎么体面地死"（HANDOFF），这集讲"该不该复活"。

## 已出局（按新标尺淘汰，存档备查）

- ~~许愿池模式~~（太泛，是话题不是行为）
- ~~否定驱动开发~~（太浅，蠢错）
- ~~验收被交付物绑架~~（机制对但自查/整改动作不够硬）
- ~~思考外包~~（软技能，无自查动作）
- ~~信任二极管~~（心态话题，已被 #5 的具体行为覆盖）
- ~~一把钥匙开所有门~~（模型分级表无硬判据）

## 讨论记录

（逐个讨论后在此回填决议）
