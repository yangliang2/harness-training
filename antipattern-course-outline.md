# 反范式课程大纲 · 《改掉这个习惯》（7 集 × ≤5 分钟）

> 状态：v1.5（2026-08-23，第 3、4 集换序 + 第 3 集主题换骨为"无脑自建 MCP vs docs as code"；spike/交付物目录沿用历史集号，映射见各集设计档）。v1.4（2026-08-21，原第 7 集"僵尸会话复活"整集出局，七讲定版不补位；耐久资产并入第 5 集收尾，决策链见 `antipattern-candidates.md` #18）。候选与出局记录见 `antipattern-candidates.md`。
> 定位：与主课（Harness Engineering）**完全独立**——零基础可看，不出现 harness/半衰期等主课概念。
> 视角：主课讲"怎么给 AI 装约束"，本课讲"人自己用 AI 的坏习惯"。
> 每集铁律：一集一个反范式（点名人常干的具体行为）；**原理只讲一句**（为什么这个病成立），不展开理论；整改动作当天可落地；诚实边界必须讲。
> 标题纪律：标题党但不吹牛——系列名《改掉这个习惯》扛主题，集名只给效果（"让 AI …"），习惯本身藏住；涉及数字的表述开拍实拍对上才算数。

## 集序与标题

| 序 | 集名 | 反模式（预告中藏住的习惯） |
|---|---|---|
| 1 | 改掉这个习惯，让 AI 立刻变聪明 | 配置只加不减 |
| 2 | 改掉这个习惯，让 AI 听得清 | 工具输出无节制 |
| 3 | 改掉这个习惯，让 AI 一学就会 | 无脑自建 MCP（2026-08-23 主题换骨 + 与第 4 集换序） |
| 4 | 改掉这个习惯，让 AI 不记错事 | 参考文档只加不理（堆料，2026-08-23 换骨定向） |
| 5 | 改掉这个习惯，让 AI 下午不变笨 | 长会话当仓库 |
| 6 | 改掉这个习惯，让 AI 不走神 | 一个会话炒三盘菜 |
| 7 | 改掉这个习惯，让 AI 说到做到 | 空头支票（压轴） |

## 串场结构

- **税篇（1-4）**：配置税 → 工具输出税 → 自建税 → 语料税（2026-08-23 换序）。钩子链："配置清了，它跑起来的输出呢"→"别人的坏工具治了，轮到你造的呢"→"知识搬进 repo 了，堆进来不等于能用"→"文档理好了，会话别糟蹋"。
- **会话篇（5-6）**：活着（到线换班）→ 分线（四出口）。
- **信任收口（7）**：前六集让信号更干净，最后一集讲信号干净了也不能信叙述——看工件。含本课程开发中的两次真实事故。

---

## 第 1 集：改掉这个习惯，让 AI 立刻变聪明（配置只加不减）
- **实测结论（2026-08-19）**：【可实拍】MCP 税数字硬（12 server/48 工具 = +28.6k token，线性 597/工具，`spike/verify/ep01-mcp-tax/`）；/doctor 无头可跑至确认前【可实拍】；Tool Search 本端点不触发——2026-08-20 决议整段砍出正片（与本集判据无因果、目标观众无认知）。

- **反模式**：MCP / skill / plugin / CLAUDE.md 只进不出。
- **原理一句**：常驻内容每一轮都在上下文里占位置——占的是它的注意力，也是你的钱。
- **翻车/对照**：同一任务，挂 15 个 MCP server vs 只留 3 个，`/context` 占用对比。
- **干货**：跑 `/doctor`（已核实官方功能：闲置 vs 上下文成本体检 + CLAUDE.md 修剪，需 v2.1.206+）；判据"上周零次使用就删"；口诀"它自己能从代码推出来的，不用你写"。
- **诚实边界**：/doctor 的 keep/remove 是建议不是圣旨——Uses 计数窗口薄（实跑仅 1 天）、token 为估算非实测，删前自己 `/context` 复核。（Tool Search 段 2026-08-20 砍出正片。）
- **交付物**：环境体检清单（含 Cursor 对应栏，2026-08-23 决议：主案例维持 Claude Code，Cursor 走真截插卡+对应栏，见 `antipattern-course/ep01.md` 讨论记录与 `research/cursor-config-parity.md`）。
- **待实测**：本机装 N 个 server 的 `/context` 占用；Tool Search 是否触发；`/doctor` 全流程实录（修剪建议为模型生成，按"配置面实录、建议走策展"）。

## 第 2 集：改掉这个习惯，让 AI 听得清（工具输出无节制）
- **实测结论（2026-08-21 终版，定稿）**：【可实拍】三段实测链：①单条长输出模型 54/54 自恢复（只差 +43% 轮次/+46% 成本），保头截断杀尾部摘要（首见：头 9/9 vs 中尾 0/18）；②MCP 抽取 hook 完全可行（Bash 三坑在 MCP 全不成立），A/B 量化：-77% 成本/-44% 轮次/-98% 单次体积，串行调用放大税（cache_read 385k vs 52.7k）；③落盘四组最低 $0.118，硬顶 ~82–103KB（超过则 hook 被 CC 持久化旁路），抽错字段崩塌实录（5/0/0 重试风暴、单次 $0.53、诚实拒答）（`spike/verify/ep02-tool-output/` + `ep02-truncation/` + `ep02-mcp-extract/`）。

- **反模式**：接了整表返回的 MCP / 输出从不节制的脚本，不加抽取层——每次调用几百字段灌进上下文，只用几个。
- **原理一句**：贵的不是长输出，是每轮注入上下文的字节 × 轮数——每一轮都在全价重读。
- **定位金句**：工具返回又臭又长 = 你的环境设计差，不是模型的病。
- **干货（分诊两条，均有实测）**：①字段可知且单次 <82KB → PostToolUse 抽取 hook（matcher `mcp__server__*`）②字段不可预知或超大 → 落盘（>82KB harness 自动接管）。
- **判据**：它每次真正用哪几个字段？答得上来就抽取，答不上来就落盘。
- **诚实边界**：抽错字段 = 亲手丢信号（崩塌实录）；isError 旁路；有全量出口时模型自己绕过抽取；compact 丢约束引第 5 集实测，不编"几轮就压缩"；机制标 v2.1.235 开拍复核。
- **自查**：`/context` 看 tool output 占比；找单次最长工具返回。
- **交付物**：抽取 hook 模板 + 落盘 hook 变体 + matcher 避坑卡。
- **待实测**：开拍当月复核 v2.1.235 机制（matcher 写法 / isError 分流 / 82KB 硬顶 / 50KB 内联阈值）。

## 第 3 集：改掉这个习惯，让 AI 一学就会（无脑自建 MCP）
- **实测结论（2026-08-23 主题换骨，完整版见 `antipattern-course/ep03.md`）**：历史两轮（help 合格自研 CLI 10/10、7/8 零工具错误、11.6 轮/$0.368 反超 gh 13.2 轮/$0.459）按两轴口径重读——自研赢在贴合非熟悉，gh 零方差红利真实（接口税转岗诚实边界）。**待跑 = KB spike 三臂 A/M0/M1**（docs as code vs 好文档 KB MCP vs 中位烂 KB MCP，千篇级夹具、任务混合与翻车阈值预登记，唯一 blocking；三出口口径预写，集子不赌 spike）。

- **反模式**：无脑自建 MCP——"要让 AI 用上 X"的第一反应是写个 MCP server（工具=MCP 条件反射），没先问它是不是已经会了。主案例：自建知识库 MCP vs docs as code。
- **原理一句**：它的"会"只有两个来源——训练时见过，或运行时读到；两个都没有，它只能编。（推论：熟悉决定翻不翻车，贴合决定跑几轮；自建 = 拿贴合收益换分布税。）
- **demo**：逐件对照表（kb_search vs Grep / kb_create·update vs Write·Edit / kb_get_history vs git log·blame——贴合红利为零的纯支出交易）+ 三臂对照（数字占位待 spike）。
- **干货（判税三问）**：①它是不是已经会了？（文件/git/grep 覆盖 → 别造；知识库 → docs as code）②必须造 → 选会自学的形态（CLI+--help 优先；兼容外壳照主流命令面，glab 照 gh 先例）③只能 MCP（远程/OAuth/无 shell 宿主）→ 文档当 prompt 写过合格线、报错也是文档；存量系统先查官方 hosted。
- **诚实边界**：三条正当例外（非工程用户 44% / 细粒度权限 / 事务性业务数据勿进 git）+ 无获胜区间挤压（小规模输文件+git、大规模输厂商产品；规模问题先上索引 = 钩下一集）；禁说清单见 `antipattern-course/ep03.md`。
- **交付物**：判税三问决策卡（正面三问 + 背面逐件对照与例外格，含裸写验收）。

## 第 4 集：改掉这个习惯，让 AI 不记错事（参考文档只加不理）
- **实测结论（2026-08-23 换骨定向，完整版见 `antipattern-course/ep04.md`）**：【方向反转】四轮 spike 29/29 压不出喂毒 + 文献三角互证（Gloaguen/Khatri：喂料动成本不动正确率）→ 反模式定为**堆料错觉**；税实证（案例组 1.5–2× 轮次；文献 +20–23% 成本、read 占 76.1%）；正确率危害条件化（测量不可得 ∧ 相似不命中密度高——Cuconasu：相似不命中最伤，随机无关反而无害）。**待跑 v2 = 索引组 vs 堆料组税 A/B**（一轮封顶预登记，唯一 blocking 项，顺带 skill 真实规模实测）。

- **反模式**：给 AI 的参考文档只加不理——以为堆得越多它学得越好（学习错觉：案例不进权重只进上下文）。隐藏层：堆的全是人视角整理的料。
- **原理一句**：它不会学——文档不进它的记忆，只进它的桌面；每个会话它都是第一天上班。
- **demo**：反转钩子（29 次投毒失败实录）+ 税 A/B（堆料组 vs 目录组轮次/成本对比）。
- **干货（三道门 + 三件套）**：判据三道门——代码里读得出的不写 / 训练时就会的不写 / 剩下的真金答"什么时候该读你"进目录；手段——doc-index-builder skill（扫 docs/ 出带证据分诊表，人只批表）+ INDEX.md 一页（`@import` 常驻 + 先查目录规则）+ 归档 deny Read 硬隔离。防复发："新文档不进目录 = 不存在。"
- **诚实边界**：策划过的准确文档收益巨大——劝退的是"未策划的堆"不是文档；"先查目录"是建议性；正确率危害只在条件区不说满；禁说清单见 `research/repo-doc-hoarding-impact.md`。
- **交付物**：doc-index-builder skill（冒烟 3/3 通过，`antipattern-course/deliverables/ep03-doc-index/`，目录沿用历史集号）+ INDEX.md 模板 + 三道门判据卡。

## 第 5 集：改掉这个习惯，让 AI 下午不变笨（长会话当仓库）
- **实测结论（2026-08-19）**：【可实拍】链式 compact ≥9 环约束整组丢失（A2 样本已落盘：11 环后 0/3，agent 坦白记不全且张冠李戴）；CLAUDE.md 组 8-9 环逐字幸存（2/2）。短链（≤7 环）丢不了——镜头按 ≥10 环设计或直接用 A2 的第 8/9 环摘要 diff（`spike/verify/ep05-compact/`）。

- **反模式**：决策/进度/约定全存对话历史，靠 /compact 无限续命。
- **原理一句**：compact 是有损压缩——每压一次，开头的约定就薄一层；而且每一轮都在为全部历史付全价。
- **反面素材**：OpenClaw 失忆案——flush 是模型自决写什么，概率机制；**机制兜底是概率的，亲手落盘是确定的**。
- **干货**：①装仪表（`/statusline` 上下文进度条，官方原生）②定阈值（60% 备交接、70% 必换班——compact 是事故不是信号）③HANDOFF.md 四格模板 ④PreCompact hook 兜底。
- **一句话**：看着表开车，到线就换班，撞墙有安全气囊。
- **收尾一句（2026-08-21，原第 7 集出局资产归并）**：任务死了就收工开新会话，新会话只读一页 HANDOFF——别把死历史拖进新任务；Anthropic 后端复活还比新开贵（1.25× 写入溢价 + resume 摧毁缓存 #42338；其余后端多只是丢折扣，措辞守此边界）。六家对照卡挂延伸阅读。
- **交付物**：HANDOFF.md 模板 + PreCompact hook 配置。
- **待实测**：compact 前后摘要对照（确定性版）。

## 第 6 集：改掉这个习惯，让 AI 不走神（一个会话炒三盘菜）
- **实测结论（2026-08-19）**：【可实拍（开拍即验）】/btw、/fork、/subtask 均无头不可用，但均为交互命令，拍摄即交互录屏；版本门槛已核（本机 2.1.226 满足）（`spike/verify/ep06-ep07/`）。

- **反模式**：支线话题全进上下文——正线稀释 + diff 混入不相关改动。
- **原理一句**：同一会话里的每句话都在抢它的注意力——支线进得越多，主线占得越少。
- **翻车 demo（确定性）**：主线中途"顺便改 README"→ 最终 diff 夹带。
- **干货（四出口，全原生）**：问→`/btw`；做且要上下文→`/fork`；做且要结果回报→`/subtask`；做且无关→新会话。
- **诚实边界**：/btw 只救"问"不救"做"；/fork、/subtask 为 v2.1.212+，开拍前复核。
- **交付物**：四出口判据卡。

## 第 7 集（压轴）：改掉这个习惯，让 AI 说到做到（空头支票）
- **实测结论（2026-08-19）**：【可实拍】双 hook 真实会话端到端全过：verify-confirm 拦截/放行/阴性对照成立，且实测抓出并修复 tool_result 冒充用户发言的真旁路（回归 11/11）；correction-guard 真实 block（transcript 留 hook_blocking_error 铁证），被拦后 agent 按指引走一次性声明（`spike/verify/ep08-hooks-e2e/`）。

- **反模式**：把"说"当"做"——汇报/声明/承诺不伴随工件变化，一律按没发生处理。
- **原理一句**：它的输出只是文字——"做完了"和"真的做完"之间，差一个可以检查的工件。
- **三形态**：A 顺手改动 / B 状态声明走私 / C 承诺性空话。**可见性不对称**："多出来"面板可见，"没发生"只能发生时刻登记。
- **真实案例**：本课程开发中两次状态走私事故（"好"被升级为确认）；规则刚写完两轮内被模型违反——规则是概率的。
- **干货**：①`/diff` + 三个高危时刻（宣布完成/话题切换/状态汇总）②correction-guard.py（Stop hook，检测用户纠正口头禅→无落盘则 block，脚本级实测 6/6）③verify-confirm.py（可选硬化，实测 4/4）。
- **诚实边界**：C 类无银弹——"没落账的承诺按不存在处理"。
- **交付物**：双 hook + settings.json 片段 + 三扳机卡。
- **待实测**：真实会话端到端 N 次（transcript 结构/拦截上限/误报率）；stop_hook_active 只拦一轮，复用主课 ep05 计数器模式。

---

## 开拍前待实测总清单

1. ep1：`/context` 实测占用、/doctor 无头段已实测（2026-08-19）；剩 /doctor 交互确认环节实拍（Tool Search 触发验证随砍段移除）
2. ep2：已实测定稿（2026-08-21）；剩开拍当月复核 v2.1.235 机制（matcher 写法 / isError 分流 / 82KB 硬顶 / 50KB 内联阈值）
3. ep4：v2 索引税 A/B（堆料组 vs 目录组，一轮封顶预登记；顺带 doc-index-builder 真实规模实测）——本集唯一 blocking 项
4. ep5：compact 摘要前后对照
5. ep7：双 hook 真实会话端到端 N 次（原第 8 集，2026-08-21 顺延改号；spike 目录仍名 ep08-hooks-e2e）
6. ep6：/fork、/subtask 版本行为复核
7. ep3：KB spike 三臂 A/M0/M1（docs as code vs 好文档 KB MCP vs 中位烂 KB MCP；千篇级夹具、任务混合与翻车阈值预登记）——本集唯一 blocking 项

## 素材资产

- `spike/anti-status-smuggle/`：correction-guard.py + verify-confirm.py（脚本级已测）
- `spike/anti-plan-diagram/`：散文 vs 状态图对照（出局集遗产，可作配料）
- `research/plan-review-practices.md`、`research/spec-visual-review-evidence.md`（出局集调研，备用）
